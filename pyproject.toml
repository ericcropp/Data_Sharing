[project]
name = "data-standard"
version = "0.2.0"
description = "Cross-institution data standard and Python utilities for simulation and experimental data exchange"
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
